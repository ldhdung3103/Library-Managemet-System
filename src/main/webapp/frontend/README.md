Frontend folder for Library-WebApp


- Serve the `frontend/` folder with any static server, for example with Python:

  For Python 3:

  ```bash
  cd frontend
  python -m http.server 8000
  ```

  Then open http://localhost:8000 in your browser.
